Run some test in a simulated dbb environment

It does not launch dbb command, only a groovy interpreter so that it does not depend on dbb installation.
Adresses POSIX compliant environment, USS compatible.
