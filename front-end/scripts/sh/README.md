# Shell Front End script
These scripts are meant to be launched from USS shell

- Wrapper shell script
  They are wrapper for providing a context to run groovy front-end script
  ```sh
  run-get-build-map.sh
  ```

- Utilities shell script
  They perform task like creating a fatJar, deploying artifact to the proper location, etc

